{
  "name": "video-processor",
  "version": "1.0.0",
  "description": "Lambda function for processing basketball game videos with MediaConvert",
  "main": "handler.js",
  "scripts": {
    "build": "tsc && esbuild handler.ts --bundle --platform=node --target=node18 --outfile=dist/handler.js"
  },
  "dependencies": {
    "@aws-sdk/client-mediaconvert": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}